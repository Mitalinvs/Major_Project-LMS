/*
rafce: create react component shortcut
Dependency: mui-MUI enhances User Experience and User Interface, formik-handle form(npm i yup: wirh formik)

npm i redux redux-thunk(middleware) react-redux(connect react to redux)
<Provider></Provider>: comes from react redux in main.jsx to wrap aaplication so that redux store is available to whole aaplication
Redux is used to maintain and update data across your applications for multiple components to share, all while remaining independent of the components
 reducers are pure functions that handle state logic, accepting the initial state and action type to update and return the state, facilitating changes in React view componentsx
*/